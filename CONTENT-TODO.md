# Content still needed

Most of the original placeholder content has since been replaced with real
menu data. What's left:

## Logo

`assets/logo.svg` is a generated placeholder wordmark (all-caps, heavy
grotesque), not the real Brewci logo. Marked with an HTML comment at both
usage sites in `index.html` (S1 and S6).

## Form fallback email

`hello@brewci.pk` in the S6 fallback line (`mailto:` link and visible text)
is a placeholder domain — replace with Brewci's real contact address.

## Menu accuracy

The full menu in the Menu portal (`#menu-portal`) and the eight items in the
S3 showcase were transcribed from menu screenshots provided during
development. Worth a final read-through against the current physical menu
before launch, in case pricing or items have changed since.
